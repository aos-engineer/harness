---
allowed-tools: Bash(plugins/aos-harness/scripts/aos_cli.sh run:*)
argument-hint: <profile> --brief <path> [--domain <name>] [--dry-run]
description: Run an AOS execution profile through the shared wrapper
---

Run the requested execution workflow through the shared wrapper.

Command output:
!`plugins/aos-harness/scripts/aos_cli.sh run $ARGUMENTS`

Summarize the workflow result, including the generated output path.
